# Page Components

## Description

This directory contains only the page-level runtime entry files and page-scoped styles used by Vite builds.

## Structure

- `<page-name>`: The entry module and page-scoped styles for a specific page.

## Notes

- Keep page-specific code inside its own page directory unless it is reused across multiple pages.
