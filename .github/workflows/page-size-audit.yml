name: Page Size Audit

on:
  push:
    branches: [main]
  pull_request:
    types: [ opened, synchronize, reopened ]
    branches: [main]

jobs:
  audit:
    name: Lighthouse Performance Audit (${{ matrix.ref_name }})
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - ref_name: current
            ref: ${{ github.event_name == 'pull_request' && github.event.pull_request.head.sha || github.sha }}
      fail-fast: false
    
    steps:
      - name: Checkout ${{ matrix.ref_name }}
        uses: actions/checkout@v6
        with:
          ref: ${{ matrix.ref }}

      - name: Copy latest .github scripts
        run: |
          # 获取最新的 .github 脚本（从触发 workflow 的分支）
          # 对于 PR: 使用 head_ref (源分支)，对于其他事件: 使用 ref_name
          BRANCH_NAME="${{ github.head_ref || github.ref_name }}"
          echo "工作流分支: $BRANCH_NAME"
          
          git fetch origin "$BRANCH_NAME"
          git checkout "origin/$BRANCH_NAME" -- .github/scripts/
          
          echo "✓ 已从 $BRANCH_NAME 分支复制最新的 .github/scripts/"

      # ========== 阶段 1: 环境准备(串行,很快) ==========
      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          run_install: false

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21

      # ========== 阶段 2: 并行执行(主要耗时部分) ==========
      - name: Parallel Build Theme and Setup Halo
        shell: bash
        run: |
          set -e
          
          # 记录环境信息
          JAVA_VERSION=$(java -version 2>&1 | head -n 1 | awk -F '"' '{print $2}')
          echo "JAVA_VERSION=${JAVA_VERSION}" >> $GITHUB_ENV
          HALO_VERSION="2.22.0-alpha.1"
          echo "HALO_VERSION=${HALO_VERSION}" >> $GITHUB_ENV
          
          mkdir -p ~/logs
          
          # ============ 任务 1: 主题构建 ============
          (
            set -e
            # lightweight timing: record task and key step timestamps (milliseconds)
            task_start=$(date +%s%3N)
            echo "TASK_START:${task_start}" > ~/logs/theme.timing

            echo "📦 [Theme] 开始构建"

            step_start=$(date +%s%3N); echo "STEP_START:pnpm-install:${step_start}" >> ~/logs/theme.timing
            pnpm install
            step_end=$(date +%s%3N); echo "STEP_END:pnpm-install:${step_end}" >> ~/logs/theme.timing

            step_start=$(date +%s%3N); echo "STEP_START:pnpm-build:${step_start}" >> ~/logs/theme.timing
            pnpm build
            step_end=$(date +%s%3N); echo "STEP_END:pnpm-build:${step_end}" >> ~/logs/theme.timing

            mkdir -p ~/builds/tmp ~/builds/target

            step_start=$(date +%s%3N); echo "STEP_START:pack-files:${step_start}" >> ~/logs/theme.timing
            cp -r . ~/builds/tmp
            mkdir -p ~/builds/tmp-included
            cd ~/builds/tmp
            cat "${{ github.workspace }}/.github/include-list.txt" | xargs -I {} cp -r --parents {} ~/builds/tmp-included/
            cd ~/builds/tmp-included
            zip -9 -r howiehz-higan-cn.zip .
            mv howiehz-higan-cn.zip ~/builds/target/
            step_end=$(date +%s%3N); echo "STEP_END:pack-files:${step_end}" >> ~/logs/theme.timing

            task_end=$(date +%s%3N); echo "TASK_END:${task_end}" >> ~/logs/theme.timing
            echo "✅ [Theme] 完成"
          ) > ~/logs/theme.log 2>&1 &
          THEME_PID=$!
          
          # ============ 任务 2: Halo 设置 ============
          (
            set -e
            # lightweight timing for Halo setup (milliseconds)
            task_start=$(date +%s%3N)
            echo "TASK_START:${task_start}" > ~/logs/halo.timing

            echo "🔧 [Halo] 开始设置"

            step_start=$(date +%s%3N); echo "STEP_START:download-jar:${step_start}" >> ~/logs/halo.timing
            # 下载 Halo
            # 优先从 GitHub Releases 下载，失败则从官方备用地址下载
            if ! wget -q "https://github.com/halo-dev/halo/releases/download/v${HALO_VERSION}/halo-${HALO_VERSION}.jar" -O ~/halo.jar; then
              wget -q "https://dl.halo.run/release/halo-${HALO_VERSION}.jar" -O ~/halo.jar
            fi
            step_end=$(date +%s%3N); echo "STEP_END:download-jar:${step_end}" >> ~/logs/halo.timing

            # 创建配置
            step_start=$(date +%s%3N); echo "STEP_START:create-config:${step_start}" >> ~/logs/halo.timing
            mkdir -p ~/.halo2
            cat > ~/.halo2/application.yaml << 'EOF'
          server:
            port: 8090
          spring:
            r2dbc:
              url: r2dbc:h2:file:///${user.home}/.halo2/db/halo?MODE=MySQL&DB_CLOSE_ON_EXIT=FALSE
              username: admin
              password: admin
            sql:
              init:
                mode: always
                platform: h2
            thymeleaf:
              cache: true
          halo:
            security:
              basic-auth:
                disabled: false
            work-dir: ${user.home}/.halo2
            external-url: http://localhost:8090
          EOF
            step_end=$(date +%s%3N); echo "STEP_END:create-config:${step_end}" >> ~/logs/halo.timing

            # 启动 Halo
            step_start=$(date +%s%3N); echo "STEP_START:start-jar:${step_start}" >> ~/logs/halo.timing
            nohup java -jar ~/halo.jar \
              --spring.config.additional-location=optional:file:${HOME}/.halo2/ \
              -Dfile.encoding=UTF-8 > ~/logs/halo-startup.log 2>&1 &
            echo $! > ~/halo.pid
            step_end=$(date +%s%3N); echo "STEP_END:start-jar:${step_end}" >> ~/logs/halo.timing

            # 等待就绪
            step_start=$(date +%s%3N); echo "STEP_START:wait-readiness:${step_start}" >> ~/logs/halo.timing
            for i in {1..60}; do
              if curl -sf http://localhost:8090/actuator/health/readiness >/dev/null 2>&1; then
                echo "✅ [Halo] 就绪 (${i}次尝试)"
                break
              fi
              [ $i -eq 60 ] && { echo "❌ [Halo] 超时"; cat ~/logs/halo-startup.log; exit 1; }
              sleep 1
            done
            step_end=$(date +%s%3N); echo "STEP_END:wait-readiness:${step_end}" >> ~/logs/halo.timing

            # 初始化系统
            step_start=$(date +%s%3N); echo "STEP_START:init-system:${step_start}" >> ~/logs/halo.timing
            echo "正在初始化 Halo 系统..."
            response=$(curl -c /tmp/cookies.txt -s http://localhost:8090/system/setup)
            csrf_token=$(echo "$response" | grep '_csrf' | sed -n 's/.*value="\([^\"]*\)".*/\1/p')

            curl -b /tmp/cookies.txt -X POST \
              http://localhost:8090/system/setup \
              -H "Content-Type: application/x-www-form-urlencoded" \
              -d "_csrf=${csrf_token}" \
              -d "language=zh-CN" \
              -d "externalUrl=http://localhost:8090" \
              -d "siteTitle=0" \
              -d "username=admin" \
              -d "email=admin@example.com" \
              -d "password=admin" \
              -d "confirmPassword=admin" \
              -L -s > /dev/null
            step_end=$(date +%s%3N); echo "STEP_END:init-system:${step_end}" >> ~/logs/halo.timing

            task_end=$(date +%s%3N); echo "TASK_END:${task_end}" >> ~/logs/halo.timing
            echo "✅ [Halo] 完成"
          ) > ~/logs/halo.log 2>&1 &
          HALO_PID=$!
          
          # ============ 等待并检查结果 ============
          echo "⏳ 等待并行任务..."
          
          wait $THEME_PID
          THEME_CODE=$?
          wait $HALO_PID
          HALO_CODE=$?
          
          if [ $THEME_CODE -ne 0 ]; then
            echo "❌ 主题构建失败"
            cat ~/logs/theme.log
            exit 1
          fi
          
          if [ $HALO_CODE -ne 0 ]; then
            echo "❌ Halo 设置失败"
            cat ~/logs/halo.log
            exit 1
          fi
          
          echo "🎉 并行任务全部成功!"
          
          # 输出第一个并行任务组的时间统计
          echo ""
          echo "⏱️  第一组并行任务时间统计 (Build & Setup):"
          for tf in ~/logs/theme.timing ~/logs/halo.timing; do
            [ -f "$tf" ] || continue
            name=$(basename "$tf" .timing)
            start=$(awk -F: '/^TASK_START:/ {print $2; exit}' "$tf" || true)
            end=$(awk -F: '/^TASK_END:/ {print $2; exit}' "$tf" || true)
            if [ -n "$start" ] && [ -n "$end" ]; then
              total_ms=$((end - start))
              total_s=$(awk -v m="$total_ms" 'BEGIN{printf "%.2f", m/1000}')
              echo "- $name: total ${total_s}s"
            else
              echo "- $name: total unknown"
            fi

            awk -F: '/^STEP_START:/ {print $2":"$3}' "$tf" | while IFS= read -r s; do
              step_name=$(echo "$s" | cut -d: -f1)
              step_start=$(echo "$s" | cut -d: -f2)
              step_end=$(awk -F: -v st="$step_name" '$1=="STEP_END" && $2==st {print $3; exit}' "$tf" || true)
              if [ -n "$step_start" ] && [ -n "$step_end" ]; then
                step_ms=$((step_end - step_start))
                step_s=$(awk -v m="$step_ms" 'BEGIN{printf "%.2f", m/1000}')
                echo "  - $step_name: ${step_s}s"
              else
                echo "  - $step_name: unknown"
              fi
            done
          done

      # ========== 阶段 3: 后续步骤(串行) ==========
      - name: Install and activate theme
        run: |
          THEME_ZIP_PATH="${HOME}/builds/target/howiehz-higan-cn.zip"
          
          if [ ! -f "$THEME_ZIP_PATH" ]; then
            echo "❌ 主题文件不存在: $THEME_ZIP_PATH"
            exit 1
          fi
          
          echo "安装主题..."
          response=$(curl -v -X POST \
            http://localhost:8090/apis/api.console.halo.run/v1alpha1/themes/install \
            -u admin:admin \
            -F "file=@${THEME_ZIP_PATH}" \
            -w "\nHTTP_CODE:%{http_code}" \
            2>&1)
          
          http_code=$(echo "$response" | grep "HTTP_CODE:" | cut -d: -f2)
          
          if [ -z "$http_code" ] || ([ "$http_code" != "200" ] && [ "$http_code" != "201" ]); then
            echo "❌ 主题安装失败 (HTTP $http_code)"
            echo "$response"
            exit 1
          fi
          
          echo "✓ 主题已安装"
          
          curl -X PUT \
            http://localhost:8090/apis/api.console.halo.run/v1alpha1/themes/howiehz-higan/activation \
            -u admin:admin \
            -s > /dev/null
          
          echo "✓ 主题已激活"
          
          curl -X PUT \
            http://localhost:8090/apis/api.console.halo.run/v1alpha1/themes/howiehz-higan/invalidate-cache \
            -u admin:admin \
            -s > /dev/null
          
          echo "✓ 缓存已清除"

      - name: Disable Halo plugins
        shell: bash
        run: |
          set -e
          
          echo "禁用插件: PluginCommentWidget, PluginSearchWidget"
          
          PIDS=()
          
          for plugin in PluginCommentWidget PluginSearchWidget; do
            (
              echo "🔧 禁用 ${plugin}"
              if curl -sX PUT \
                "http://localhost:8090/apis/api.console.halo.run/v1alpha1/plugins/${plugin}/plugin-state" \
                -u admin:admin \
                -H "Content-Type: application/json" \
                -d '{"async":false,"enabled":false}'; then
                echo "✅ ${plugin} 禁用成功"
              else
                echo "⚠️  ${plugin} 禁用失败(已忽略)"
              fi
            ) &
            PIDS+=($!)
          done
          
          echo "⏳ 等待插件禁用任务..."
          
          for pid in "${PIDS[@]}"; do
            wait $pid || echo "⚠️ background job $pid exited with status $?"
          done
          
          echo "🎉 插件禁用完成"

      - name: Run Lighthouse CI and Playwright screenshots in parallel
        shell: bash
        run: |
          set -e
          mkdir -p ~/logs

          # ============ Task A: Lighthouse CI ============
          (
            set -e
            # timing for LHCI task (milliseconds)
            task_start=$(date +%s%3N)
            echo "TASK_START:${task_start}" > ~/logs/lhci.timing

            echo "📦 [LHCI] Install LHCI"
            step_start=$(date +%s%3N); echo "STEP_START:check-lhci:${step_start}" >> ~/logs/lhci.timing
            if ! command -v lhci >/dev/null 2>&1; then
              pnpm add -g @lhci/cli@0.15.x || echo "⚠️  LHCI install failed, continuing"
            else
              echo "✓ lhci already installed"
            fi
            step_end=$(date +%s%3N); echo "STEP_END:check-lhci:${step_end}" >> ~/logs/lhci.timing
            echo "LHCI_VERSION=$(lhci --version 2>/dev/null || echo 'unknown')" >> $GITHUB_ENV

            echo "🚀 [LHCI] Run Lighthouse CI"
            step_start=$(date +%s%3N); echo "STEP_START:lhci-run:${step_start}" >> ~/logs/lhci.timing
            mkdir -p ./reports
            lhci autorun \
              --collect.url=http://localhost:8090/ \
              --collect.url=http://localhost:8090/archives \
              --collect.url=http://localhost:8090/archives/hello-halo \
              --collect.url=http://localhost:8090/tags \
              --collect.url=http://localhost:8090/tags/halo \
              --collect.url=http://localhost:8090/categories \
              --collect.url=http://localhost:8090/categories/default \
              --collect.url=http://localhost:8090/authors/admin \
              --collect.url=http://localhost:8090/about \
              --collect.numberOfRuns=1 \
              --collect.settings.preset=desktop \
              --collect.settings.throttling.rttMs=0 \
              --collect.settings.throttling.throughputKbps=0 \
              --collect.settings.throttling.cpuSlowdownMultiplier=1 \
              --collect.settings.throttling.requestLatencyMs=0 \
              --collect.settings.throttling.downloadThroughputKbps=0 \
              --collect.settings.throttling.uploadThroughputKbps=0 \
              --upload.target=filesystem \
              --upload.outputDir=.lighthouseci || {
                echo "❌ [LHCI] Lighthouse CI failed"
                ls -la ./.lighthouseci/ || echo ".lighthouseci directory not found"
                exit 1
              }
            step_end=$(date +%s%3N); echo "STEP_END:lhci-run:${step_end}" >> ~/logs/lhci.timing
            task_end=$(date +%s%3N); echo "TASK_END:${task_end}" >> ~/logs/lhci.timing
            echo "✅ [LHCI] Complete"
          ) > ~/logs/lhci.log 2>&1 &
          LHCI_PID=$!

          # ============ Task B: Playwright + Argos ============
          (
            set -e
            # timing for Playwright task (milliseconds)
            task_start=$(date +%s%3N)
            echo "TASK_START:${task_start}" > ~/logs/playwright.timing

            echo "📦 [Playwright] Install Playwright and Argos"
            step_start=$(date +%s%3N); echo "STEP_START:check-argos:${step_start}" >> ~/logs/playwright.timing
            if ! command -v argos >/dev/null 2>&1; then
              pnpm add -g @argos-ci/cli || echo "⚠️  Argos CLI install failed, continuing"
            else
              echo "✓ argos already installed"
            fi
            step_end=$(date +%s%3N); echo "STEP_END:check-argos:${step_end}" >> ~/logs/playwright.timing

            step_start=$(date +%s%3N); echo "STEP_START:check-playwright:${step_start}" >> ~/logs/playwright.timing
            if ! pnpm exec -- playwright --version >/dev/null 2>&1; then
              pnpm add -w -D playwright @argos-ci/playwright || echo "⚠️  Playwright install failed, continuing"
            else
              echo "✓ playwright already available via pnpm exec"
            fi
            step_end=$(date +%s%3N); echo "STEP_END:check-playwright:${step_end}" >> ~/logs/playwright.timing

            step_start=$(date +%s%3N); echo "STEP_START:install-browsers:${step_start}" >> ~/logs/playwright.timing
            pnpm exec playwright install --with-deps chromium
            step_end=$(date +%s%3N); echo "STEP_END:install-browsers:${step_end}" >> ~/logs/playwright.timing

            echo "📸 [Playwright] Take screenshots"
            step_start=$(date +%s%3N); echo "STEP_START:screenshots:${step_start}" >> ~/logs/playwright.timing
            mkdir -p screenshots
            node .github/scripts/playwright-screenshots.js
            step_end=$(date +%s%3N); echo "STEP_END:screenshots:${step_end}" >> ~/logs/playwright.timing

            echo "☁️ [Playwright] Upload screenshots to Argos"
            step_start=$(date +%s%3N); echo "STEP_START:upload-argos:${step_start}" >> ~/logs/playwright.timing
            if [ -n "${{ secrets.ARGOS_TOKEN }}" ]; then
              argos upload --token ${{ secrets.ARGOS_TOKEN }} ./screenshots/ || echo "⚠️ Argos upload failed"
            else
              echo "ℹ️  ARGOS_TOKEN not set; skipping Argos upload"
            fi
            step_end=$(date +%s%3N); echo "STEP_END:upload-argos:${step_end}" >> ~/logs/playwright.timing

            task_end=$(date +%s%3N); echo "TASK_END:${task_end}" >> ~/logs/playwright.timing
            echo "✅ [Playwright] Complete"
          ) > ~/logs/playwright.log 2>&1 &
          PLAY_PID=$!

          echo "⏳ Waiting parallel tasks..."
          wait $LHCI_PID
          LHCI_CODE=$?
          wait $PLAY_PID
          PLAY_CODE=$?

          if [ $LHCI_CODE -ne 0 ]; then
            echo "❌ LHCI task failed. Log output:"
            if [ -f ~/logs/lhci.log ]; then
              sed -n '1,200p' ~/logs/lhci.log
            else
              echo "ℹ️  日志文件不存在: ~/logs/lhci.log"
            fi
            exit $LHCI_CODE
          fi

          if [ $PLAY_CODE -ne 0 ]; then
            echo "❌ Playwright task failed. Log output:"
            if [ -f ~/logs/playwright.log ]; then
              sed -n '1,200p' ~/logs/playwright.log
            else
              echo "ℹ️  日志文件不存在: ~/logs/playwright.log"
            fi
            exit $PLAY_CODE
          fi

          echo "🎉 Both LHCI and Playwright tasks succeeded"
          
          # 只输出第二组并行任务的时间统计
          echo ""
          echo "⏱️  第二组并行任务时间统计 (LHCI & Playwright):"
          for tf in ~/logs/lhci.timing ~/logs/playwright.timing; do
            [ -f "$tf" ] || continue
            name=$(basename "$tf" .timing)
            start=$(awk -F: '/^TASK_START:/ {print $2; exit}' "$tf" || true)
            end=$(awk -F: '/^TASK_END:/ {print $2; exit}' "$tf" || true)
            if [ -n "$start" ] && [ -n "$end" ]; then
              total_ms=$((end - start))
              total_s=$(awk -v m="$total_ms" 'BEGIN{printf "%.2f", m/1000}')
              echo "- $name: total ${total_s}s"
            else
              echo "- $name: total unknown"
            fi

            awk -F: '/^STEP_START:/ {print $2":"$3}' "$tf" | while IFS= read -r s; do
              step_name=$(echo "$s" | cut -d: -f1)
              step_start=$(echo "$s" | cut -d: -f2)
              step_end=$(awk -F: -v st="$step_name" '$1=="STEP_END" && $2==st {print $3; exit}' "$tf" || true)
              if [ -n "$step_start" ] && [ -n "$step_end" ]; then
                step_ms=$((step_end - step_start))
                step_s=$(awk -v m="$step_ms" 'BEGIN{printf "%.2f", m/1000}')
                echo "  - $step_name: ${step_s}s"
              else
                echo "  - $step_name: unknown"
              fi
            done
          done
      
      - name: Upload screenshots (${{ matrix.ref_name }})
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: screenshots-${{ matrix.ref_name }}
          path: ./screenshots/
          include-hidden-files: true
          if-no-files-found: warn
        
      - name: Upload Lighthouse raw data (${{ matrix.ref_name }})
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: lighthouse-raw-data-${{ matrix.ref_name }}
          path: ./.lighthouseci/
          include-hidden-files: true
          if-no-files-found: warn

      - name: Generate reports (${{ matrix.ref_name }})
        if: success()
        env:
          OUTPUT_DIR: ./reports
          LIGHTHOUSE_RESULTS_DIR: ./.lighthouseci
          HALO_VERSION: ${{ env.HALO_VERSION }}
          JAVA_VERSION: ${{ env.JAVA_VERSION }}
          THEME_VERSION: ${{ matrix.ref }}
          GITHUB_SHA: ${{ github.sha }}
          LHCI_VERSION: ${{ env.LHCI_VERSION }}
          REF_NAME: ${{ matrix.ref_name }}
        run: |
          if [ ! -d "./.lighthouseci" ]; then
            echo "⚠️  警告：.lighthouseci 目录不存在"
            exit 1
          fi
          
          json_count=$(find ./.lighthouseci -name "lhr-*.json" | wc -l)
          if [ "$json_count" -eq 0 ]; then
            echo "⚠️  警告：没有找到 Lighthouse 结果文件"
            exit 1
          fi
          
          echo "找到 $json_count 个 Lighthouse 结果文件"
          node .github/scripts/generate-report.js

      - name: Upload reports (${{ matrix.ref_name }})
        uses: actions/upload-artifact@v4
        with:
          name: page-size-reports-${{ matrix.ref_name }}
          path: ./reports/

      - name: Stop Halo
        if: always()
        run: |
          if [ -f ~/halo.pid ]; then
            kill "$(cat ~/halo.pid)" 2>/dev/null || echo "⚠️ kill failed (maybe already stopped)"
          fi

      - name: Upload Halo logs
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: halo-logs-${{ matrix.ref_name }}
          path: ~/logs/

  audit-base:
    name: Lighthouse Performance Audit (${{ matrix.ref_name }})
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - ref_name: base
            ref: ${{ github.event_name == 'pull_request' && github.event.pull_request.base.sha || github.sha }}
      fail-fast: false
    steps:
      - name: Checkout ${{ matrix.ref_name }}
        uses: actions/checkout@v6
        with:
          ref: ${{ matrix.ref }}

      - name: Copy latest .github scripts
        run: |
          # 获取最新的 .github 脚本（从触发 workflow 的分支）
          # 对于 PR: 使用 head_ref (源分支)，对于其他事件: 使用 ref_name
          BRANCH_NAME="${{ github.head_ref || github.ref_name }}"
          echo "工作流分支: $BRANCH_NAME"
          
          git fetch origin "$BRANCH_NAME"
          git checkout "origin/$BRANCH_NAME" -- .github/scripts/
          
          echo "✓ 已从 $BRANCH_NAME 分支复制最新的 .github/scripts/"

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          run_install: false

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21

      - name: Parallel Build Theme and Setup Halo
        shell: bash
        run: |
          set -e
          
          # 记录环境信息
          JAVA_VERSION=$(java -version 2>&1 | head -n 1 | awk -F '"' '{print $2}')
          echo "JAVA_VERSION=${JAVA_VERSION}" >> $GITHUB_ENV
          HALO_VERSION="2.22.0-alpha.1"
          echo "HALO_VERSION=${HALO_VERSION}" >> $GITHUB_ENV
          
          mkdir -p ~/logs
          
          # ============ 任务 1: 主题构建 ============
          (
            set -e
            task_start=$(date +%s%3N)
            echo "TASK_START:${task_start}" > ~/logs/theme.timing

            echo "📦 [Theme] 开始构建"

            step_start=$(date +%s%3N); echo "STEP_START:pnpm-install:${step_start}" >> ~/logs/theme.timing
            pnpm install
            step_end=$(date +%s%3N); echo "STEP_END:pnpm-install:${step_end}" >> ~/logs/theme.timing

            step_start=$(date +%s%3N); echo "STEP_START:pnpm-build:${step_start}" >> ~/logs/theme.timing
            pnpm build
            step_end=$(date +%s%3N); echo "STEP_END:pnpm-build:${step_end}" >> ~/logs/theme.timing

            mkdir -p ~/builds/tmp ~/builds/target

            step_start=$(date +%s%3N); echo "STEP_START:pack-files:${step_start}" >> ~/logs/theme.timing
            cp -r . ~/builds/tmp
            mkdir -p ~/builds/tmp-included
            cd ~/builds/tmp
            cat "${{ github.workspace }}/.github/include-list.txt" | xargs -I {} cp -r --parents {} ~/builds/tmp-included/
            cd ~/builds/tmp-included
            zip -9 -r howiehz-higan-cn.zip .
            mv howiehz-higan-cn.zip ~/builds/target/
            step_end=$(date +%s%3N); echo "STEP_END:pack-files:${step_end}" >> ~/logs/theme.timing

            task_end=$(date +%s%3N); echo "TASK_END:${task_end}" >> ~/logs/theme.timing
            echo "✅ [Theme] 完成"
          ) > ~/logs/theme.log 2>&1 &
          THEME_PID=$!
          
          # ============ 任务 2: Halo 设置 ============
          (
            set -e
            task_start=$(date +%s%3N)
            echo "TASK_START:${task_start}" > ~/logs/halo.timing

            echo "🔧 [Halo] 开始设置"

            step_start=$(date +%s%3N); echo "STEP_START:download-jar:${step_start}" >> ~/logs/halo.timing
            if ! wget -q "https://github.com/halo-dev/halo/releases/download/v${HALO_VERSION}/halo-${HALO_VERSION}.jar" -O ~/halo.jar; then
              wget -q "https://dl.halo.run/release/halo-${HALO_VERSION}.jar" -O ~/halo.jar
            fi
            step_end=$(date +%s%3N); echo "STEP_END:download-jar:${step_end}" >> ~/logs/halo.timing

            step_start=$(date +%s%3N); echo "STEP_START:create-config:${step_start}" >> ~/logs/halo.timing
            mkdir -p ~/.halo2
            cat > ~/.halo2/application.yaml << 'EOF'
          server:
            port: 8090
          spring:
            r2dbc:
              url: r2dbc:h2:file:///${user.home}/.halo2/db/halo?MODE=MySQL&DB_CLOSE_ON_EXIT=FALSE
              username: admin
              password: admin
            sql:
              init:
                mode: always
                platform: h2
            thymeleaf:
              cache: true
          halo:
            security:
              basic-auth:
                disabled: false
            work-dir: ${user.home}/.halo2
            external-url: http://localhost:8090
          EOF
            step_end=$(date +%s%3N); echo "STEP_END:create-config:${step_end}" >> ~/logs/halo.timing

            step_start=$(date +%s%3N); echo "STEP_START:start-jar:${step_start}" >> ~/logs/halo.timing
            nohup java -jar ~/halo.jar \
              --spring.config.additional-location=optional:file:${HOME}/.halo2/ \
              -Dfile.encoding=UTF-8 > ~/logs/halo-startup.log 2>&1 &
            echo $! > ~/halo.pid
            step_end=$(date +%s%3N); echo "STEP_END:start-jar:${step_end}" >> ~/logs/halo.timing

            step_start=$(date +%s%3N); echo "STEP_START:wait-readiness:${step_start}" >> ~/logs/halo.timing
            for i in {1..60}; do
              if curl -sf http://localhost:8090/actuator/health/readiness >/dev/null 2>&1; then
                echo "✅ [Halo] 就绪 (${i}次尝试)"
                break
              fi
              [ $i -eq 60 ] && { echo "❌ [Halo] 超时"; cat ~/logs/halo-startup.log; exit 1; }
              sleep 1
            done
            step_end=$(date +%s%3N); echo "STEP_END:wait-readiness:${step_end}" >> ~/logs/halo.timing

            step_start=$(date +%s%3N); echo "STEP_START:init-system:${step_start}" >> ~/logs/halo.timing
            echo "正在初始化 Halo 系统..."
            response=$(curl -c /tmp/cookies.txt -s http://localhost:8090/system/setup)
            csrf_token=$(echo "$response" | grep '_csrf' | sed -n 's/.*value="\([^\"]*\)".*/\1/p')

            curl -b /tmp/cookies.txt -X POST \
              http://localhost:8090/system/setup \
              -H "Content-Type: application/x-www-form-urlencoded" \
              -d "_csrf=${csrf_token}" \
              -d "language=zh-CN" \
              -d "externalUrl=http://localhost:8090" \
              -d "siteTitle=0" \
              -d "username=admin" \
              -d "email=admin@example.com" \
              -d "password=admin" \
              -d "confirmPassword=admin" \
              -L -s > /dev/null
            step_end=$(date +%s%3N); echo "STEP_END:init-system:${step_end}" >> ~/logs/halo.timing

            task_end=$(date +%s%3N); echo "TASK_END:${task_end}" >> ~/logs/halo.timing
            echo "✅ [Halo] 完成"
          ) > ~/logs/halo.log 2>&1 &
          HALO_PID=$!

          echo "⏳ 等待并行任务..."
          
          wait $THEME_PID
          THEME_CODE=$?
          wait $HALO_PID
          HALO_CODE=$?
          
          if [ $THEME_CODE -ne 0 ]; then
            echo "❌ 主题构建失败"
            cat ~/logs/theme.log
            exit 1
          fi
          
          if [ $HALO_CODE -ne 0 ]; then
            echo "❌ Halo 设置失败"
            cat ~/logs/halo.log
            exit 1
          fi
          
          echo "🎉 并行任务全部成功!"
          
          # 输出第一个并行任务组的时间统计
          echo ""
          echo "⏱️  第一组并行任务时间统计 (Build & Setup):"
          for tf in ~/logs/theme.timing ~/logs/halo.timing; do
            [ -f "$tf" ] || continue
            name=$(basename "$tf" .timing)
            start=$(awk -F: '/^TASK_START:/ {print $2; exit}' "$tf" || true)
            end=$(awk -F: '/^TASK_END:/ {print $2; exit}' "$tf" || true)
            if [ -n "$start" ] && [ -n "$end" ]; then
              total_ms=$((end - start))
              total_s=$(awk -v m="$total_ms" 'BEGIN{printf "%.2f", m/1000}')
              echo "- $name: total ${total_s}s"
            else
              echo "- $name: total unknown"
            fi

            awk -F: '/^STEP_START:/ {print $2":"$3}' "$tf" | while IFS= read -r s; do
              step_name=$(echo "$s" | cut -d: -f1)
              step_start=$(echo "$s" | cut -d: -f2)
              step_end=$(awk -F: -v st="$step_name" '$1=="STEP_END" && $2==st {print $3; exit}' "$tf" || true)
              if [ -n "$step_start" ] && [ -n "$step_end" ]; then
                step_ms=$((step_end - step_start))
                step_s=$(awk -v m="$step_ms" 'BEGIN{printf "%.2f", m/1000}')
                echo "  - $step_name: ${step_s}s"
              else
                echo "  - $step_name: unknown"
              fi
            done
          done

      - name: Install and activate theme
        run: |
          THEME_ZIP_PATH="${HOME}/builds/target/howiehz-higan-cn.zip"
          
          if [ ! -f "$THEME_ZIP_PATH" ]; then
            echo "❌ 主题文件不存在: $THEME_ZIP_PATH"
            exit 1
          fi
          
          echo "安装主题..."
          response=$(curl -v -X POST \
            http://localhost:8090/apis/api.console.halo.run/v1alpha1/themes/install \
            -u admin:admin \
            -F "file=@${THEME_ZIP_PATH}" \
            -w "\nHTTP_CODE:%{http_code}" \
            2>&1)
          
          http_code=$(echo "$response" | grep "HTTP_CODE:" | cut -d: -f2)
          
          if [ -z "$http_code" ] || ([ "$http_code" != "200" ] && [ "$http_code" != "201" ]); then
            echo "❌ 主题安装失败 (HTTP $http_code)"
            echo "$response"
            exit 1
          fi
          
          echo "✓ 主题已安装"
          
          curl -X PUT \
            http://localhost:8090/apis/api.console.halo.run/v1alpha1/themes/howiehz-higan/activation \
            -u admin:admin \
            -s > /dev/null
          
          echo "✓ 主题已激活"
          
          curl -X PUT \
            http://localhost:8090/apis/api.console.halo.run/v1alpha1/themes/howiehz-higan/invalidate-cache \
            -u admin:admin \
            -s > /dev/null
          
          echo "✓ 缓存已清除"

      - name: Disable Halo plugins
        shell: bash
        run: |
          set -e
          
          echo "禁用插件: PluginCommentWidget, PluginSearchWidget"
          
          PIDS=()
          
          for plugin in PluginCommentWidget PluginSearchWidget; do
            (
              echo "🔧 禁用 ${plugin}"
              if curl -sX PUT \
                "http://localhost:8090/apis/api.console.halo.run/v1alpha1/plugins/${plugin}/plugin-state" \
                -u admin:admin \
                -H "Content-Type: application/json" \
                -d '{"async":false,"enabled":false}'; then
                echo "✅ ${plugin} 禁用成功"
              else
                echo "⚠️  ${plugin} 禁用失败(已忽略)"
              fi
            ) &
            PIDS+=($!)
          done
          
          echo "⏳ 等待插件禁用任务..."
          
          for pid in "${PIDS[@]}"; do
            wait $pid || echo "⚠️ background job $pid exited with status $?"
          done
          
          echo "🎉 插件禁用完成"

      - name: Run Lighthouse CI and Playwright screenshots in parallel
        shell: bash
        run: |
          set -e
          mkdir -p ~/logs

          # ============ Task A: Lighthouse CI ============
          (
            set -e
            task_start=$(date +%s%3N)
            echo "TASK_START:${task_start}" > ~/logs/lhci.timing

            echo "📦 [LHCI] Install LHCI"
            step_start=$(date +%s%3N); echo "STEP_START:check-lhci:${step_start}" >> ~/logs/lhci.timing
            if ! command -v lhci >/dev/null 2>&1; then
              pnpm add -g @lhci/cli@0.15.x || echo "⚠️  LHCI install failed, continuing"
            else
              echo "✓ lhci already installed"
            fi
            step_end=$(date +%s%3N); echo "STEP_END:check-lhci:${step_end}" >> ~/logs/lhci.timing
            echo "LHCI_VERSION=$(lhci --version 2>/dev/null || echo 'unknown')" >> $GITHUB_ENV

            echo "🚀 [LHCI] Run Lighthouse CI"
            step_start=$(date +%s%3N); echo "STEP_START:lhci-run:${step_start}" >> ~/logs/lhci.timing
            mkdir -p ./reports
            lhci autorun \
              --collect.url=http://localhost:8090/ \
              --collect.url=http://localhost:8090/archives \
              --collect.url=http://localhost:8090/archives/hello-halo \
              --collect.url=http://localhost:8090/tags \
              --collect.url=http://localhost:8090/tags/halo \
              --collect.url=http://localhost:8090/categories \
              --collect.url=http://localhost:8090/categories/default \
              --collect.url=http://localhost:8090/authors/admin \
              --collect.url=http://localhost:8090/about \
              --collect.numberOfRuns=1 \
              --collect.settings.preset=desktop \
              --collect.settings.throttling.rttMs=0 \
              --collect.settings.throttling.throughputKbps=0 \
              --collect.settings.throttling.cpuSlowdownMultiplier=1 \
              --collect.settings.throttling.requestLatencyMs=0 \
              --collect.settings.throttling.downloadThroughputKbps=0 \
              --collect.settings.throttling.uploadThroughputKbps=0 \
              --upload.target=filesystem \
              --upload.outputDir=.lighthouseci || {
                echo "❌ [LHCI] Lighthouse CI failed"
                ls -la ./.lighthouseci/ || echo ".lighthouseci directory not found"
                exit 1
              }
            step_end=$(date +%s%3N); echo "STEP_END:lhci-run:${step_end}" >> ~/logs/lhci.timing
            task_end=$(date +%s%3N); echo "TASK_END:${task_end}" >> ~/logs/lhci.timing
            echo "✅ [LHCI] Complete"
          ) > ~/logs/lhci.log 2>&1 &
          LHCI_PID=$!

          # ============ Task B: Playwright + Argos ============
          (
            set -e
            task_start=$(date +%s%3N)
            echo "TASK_START:${task_start}" > ~/logs/playwright.timing

            echo "📦 [Playwright] Install Playwright and Argos"
            step_start=$(date +%s%3N); echo "STEP_START:check-argos:${step_start}" >> ~/logs/playwright.timing
            if ! command -v argos >/dev/null 2>&1; then
              pnpm add -g @argos-ci/cli || echo "⚠️  Argos CLI install failed, continuing"
            else
              echo "✓ argos already installed"
            fi
            step_end=$(date +%s%3N); echo "STEP_END:check-argos:${step_end}" >> ~/logs/playwright.timing

            step_start=$(date +%s%3N); echo "STEP_START:check-playwright:${step_start}" >> ~/logs/playwright.timing
            if ! pnpm exec -- playwright --version >/dev/null 2>&1; then
              pnpm add -w -D playwright @argos-ci/playwright || echo "⚠️  Playwright install failed, continuing"
            else
              echo "✓ playwright already available via pnpm exec"
            fi
            step_end=$(date +%s%3N); echo "STEP_END:check-playwright:${step_end}" >> ~/logs/playwright.timing

            step_start=$(date +%s%3N); echo "STEP_START:install-browsers:${step_start}" >> ~/logs/playwright.timing
            pnpm exec playwright install --with-deps chromium
            step_end=$(date +%s%3N); echo "STEP_END:install-browsers:${step_end}" >> ~/logs/playwright.timing

            echo "📸 [Playwright] Take screenshots"
            step_start=$(date +%s%3N); echo "STEP_START:screenshots:${step_start}" >> ~/logs/playwright.timing
            mkdir -p screenshots
            node .github/scripts/playwright-screenshots.js
            step_end=$(date +%s%3N); echo "STEP_END:screenshots:${step_end}" >> ~/logs/playwright.timing

            echo "☁️ [Playwright] Upload screenshots to Argos"
            step_start=$(date +%s%3N); echo "STEP_START:upload-argos:${step_start}" >> ~/logs/playwright.timing
            if [ -n "${{ secrets.ARGOS_TOKEN }}" ]; then
              argos upload --token ${{ secrets.ARGOS_TOKEN }} ./screenshots/ || echo "⚠️ Argos upload failed"
            else
              echo "ℹ️  ARGOS_TOKEN not set; skipping Argos upload"
            fi
            step_end=$(date +%s%3N); echo "STEP_END:upload-argos:${step_end}" >> ~/logs/playwright.timing

            task_end=$(date +%s%3N); echo "TASK_END:${task_end}" >> ~/logs/playwright.timing
            echo "✅ [Playwright] Complete"
          ) > ~/logs/playwright.log 2>&1 &
          PLAY_PID=$!

          echo "⏳ Waiting parallel tasks..."
          wait $LHCI_PID
          LHCI_CODE=$?
          wait $PLAY_PID
          PLAY_CODE=$?

          if [ $LHCI_CODE -ne 0 ]; then
            echo "❌ LHCI task failed. Log output:"
            if [ -f ~/logs/lhci.log ]; then
              sed -n '1,200p' ~/logs/lhci.log
            else
              echo "ℹ️  日志文件不存在: ~/logs/lhci.log"
            fi
            exit $LHCI_CODE
          fi

          if [ $PLAY_CODE -ne 0 ]; then
            echo "❌ Playwright task failed. Log output:"
            if [ -f ~/logs/playwright.log ]; then
              sed -n '1,200p' ~/logs/playwright.log
            else
              echo "ℹ️  日志文件不存在: ~/logs/playwright.log"
            fi
            exit $PLAY_CODE
          fi

          echo "🎉 Both LHCI and Playwright tasks succeeded"
          
          # 只输出第二组并行任务的时间统计
          echo ""
          echo "⏱️  第二组并行任务时间统计 (LHCI & Playwright):"
          for tf in ~/logs/lhci.timing ~/logs/playwright.timing; do
            [ -f "$tf" ] || continue
            name=$(basename "$tf" .timing)
            start=$(awk -F: '/^TASK_START:/ {print $2; exit}' "$tf" || true)
            end=$(awk -F: '/^TASK_END:/ {print $2; exit}' "$tf" || true)
            if [ -n "$start" ] && [ -n "$end" ]; then
              total_ms=$((end - start))
              total_s=$(awk -v m="$total_ms" 'BEGIN{printf "%.2f", m/1000}')
              echo "- $name: total ${total_s}s"
            else
              echo "- $name: total unknown"
            fi

            awk -F: '/^STEP_START:/ {print $2":"$3}' "$tf" | while IFS= read -r s; do
              step_name=$(echo "$s" | cut -d: -f1)
              step_start=$(echo "$s" | cut -d: -f2)
              step_end=$(awk -F: -v st="$step_name" '$1=="STEP_END" && $2==st {print $3; exit}' "$tf" || true)
              if [ -n "$step_start" ] && [ -n "$step_end" ]; then
                step_ms=$((step_end - step_start))
                step_s=$(awk -v m="$step_ms" 'BEGIN{printf "%.2f", m/1000}')
                echo "  - $step_name: ${step_s}s"
              else
                echo "  - $step_name: unknown"
              fi
            done
          done
      
      - name: Upload screenshots (${{ matrix.ref_name }})
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: screenshots-${{ matrix.ref_name }}
          path: ./screenshots/
          include-hidden-files: true
          if-no-files-found: warn
        
      - name: Upload Lighthouse raw data (${{ matrix.ref_name }})
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: lighthouse-raw-data-${{ matrix.ref_name }}
          path: ./.lighthouseci/
          include-hidden-files: true
          if-no-files-found: warn

      - name: Generate reports (${{ matrix.ref_name }})
        if: success()
        env:
          OUTPUT_DIR: ./reports
          LIGHTHOUSE_RESULTS_DIR: ./.lighthouseci
          HALO_VERSION: ${{ env.HALO_VERSION }}
          JAVA_VERSION: ${{ env.JAVA_VERSION }}
          THEME_VERSION: ${{ matrix.ref }}
          GITHUB_SHA: ${{ github.sha }}
          LHCI_VERSION: ${{ env.LHCI_VERSION }}
          REF_NAME: ${{ matrix.ref_name }}
        run: |
          if [ ! -d "./.lighthouseci" ]; then
            echo "⚠️  警告：.lighthouseci 目录不存在"
            exit 1
          fi
          
          json_count=$(find ./.lighthouseci -name "lhr-*.json" | wc -l)
          if [ "$json_count" -eq 0 ]; then
            echo "⚠️  警告：没有找到 Lighthouse 结果文件"
            exit 1
          fi
          
          echo "找到 $json_count 个 Lighthouse 结果文件"
          node .github/scripts/generate-report.js

      - name: Upload reports (${{ matrix.ref_name }})
        uses: actions/upload-artifact@v4
        with:
          name: page-size-reports-${{ matrix.ref_name }}
          path: ./reports/

      - name: Stop Halo
        if: always()
        run: |
          if [ -f ~/halo.pid ]; then
            kill "$(cat ~/halo.pid)" 2>/dev/null || echo "⚠️ kill failed (maybe already stopped)"
          fi

      - name: Upload Halo logs
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: halo-logs-${{ matrix.ref_name }}
          path: ~/logs/

  compare-and-comment:
    name: Compare and Comment
    needs: [audit, audit-base]
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    permissions:  # use permissions because use of pr-comments
      contents: read
      pull-requests: write
    
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
      
      - name: Download current reports
        uses: actions/download-artifact@v4
        with:
          name: page-size-reports-current
          path: ./reports-current
      
      - name: Download base reports
        uses: actions/download-artifact@v4
        with:
          name: page-size-reports-base
          path: ./reports-base
      
      - name: Generate comparison report
        run: |
          node .github/scripts/compare-reports.js
        env:
          CURRENT_REPORT: ./reports-current/page-size-report.json
          BASE_REPORT: ./reports-base/page-size-report.json
          OUTPUT_FILE: ./comparison-report.md
      
      - name: Comment on PR
        uses: actions/github-script@v7
        continue-on-error: true
        with:
          script: |
            const fs = require('fs');
            const report = fs.readFileSync('./comparison-report.md', 'utf8');
            
            try {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                body: report
              });
              console.log('✓ 成功发布评论');
            } catch (error) {
              if (error.status === 403) {
                console.log('⚠️  无法发布评论（权限不足）。这通常发生在 Dependabot 或其他 bot 创建的 PR 中。');
                console.log('📊 报告已生成并上传为 artifact，可以手动查看。');
              } else {
                throw error;
              }
            }
